{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "public/app" }
    }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "https://doan2-j4en.onrender.com/api/$1" },
    { "src": "^((?!.*\\.).*)$", "dest": "/index.html" }
  ]
}
